import { Chakra_Petch, Poppins } from 'next/font/google'

export const content = Poppins({
  weight: '300',
  subsets: ['latin']
})

export const heading = Poppins({
  weight: '600',
  subsets: ['latin']
})

export const logo = Chakra_Petch({
  weight: '600',
  subsets: ['latin']
})