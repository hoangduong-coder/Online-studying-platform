import { Chakra_Petch, Poppins, Roboto } from 'next/font/google'

export const content = Poppins({
  weight: '400',
  subsets: ['latin']
})

export const heading = Poppins({
  weight: '600',
  subsets: ['latin']
})

export const logo = Chakra_Petch({
  weight: '600',
  subsets: ['latin']
})